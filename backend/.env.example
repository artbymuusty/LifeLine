# 🚨 LifeLine Server Configuration Example
# Copy this file to .env and configure details

# Server Port
PORT=3000

# MS SQL Database configuration
DB_HOST=localhost
DB_PORT=1433
DB_NAME=acil_yardim_uygulamasi
DB_USER=api_user
DB_PASS=your_db_password

# JWT Authentication secret (Must be strong in production!)
JWT_SECRET=super_secret_jwt_sign_key_123

# Environment
NODE_ENV=development
